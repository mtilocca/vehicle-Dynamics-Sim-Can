#pragma once
// Minimal Zephyr log.h stub — all LOG_* expand to nothing.
#define LOG_MODULE_REGISTER(...)  do {} while(0)
#define LOG_MODULE_DECLARE(...)   do {} while(0)
#define LOG_INF(...)  do {} while(0)
#define LOG_WRN(...)  do {} while(0)
#define LOG_ERR(...)  do {} while(0)
#define LOG_DBG(...)  do {} while(0)
