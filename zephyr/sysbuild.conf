# sysbuild.conf — Zephyr sysbuild configuration
# Instructs west to build MCUboot as a companion image and flash it
# alongside the application. Requires: west build --sysbuild

SB_CONFIG_BOOTLOADER_MCUBOOT=y
